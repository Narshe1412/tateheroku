<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <title>
    <%= title %>
  </title>
  <%include scripts%>
</head>

<body>
  <%include header%>
    <div class="row">
      <% for (var i=0; i<artists.length; i++) { %>
        <div class="col-sm-3">
          <div class="card" style="width: 20rem;">
            <div class="card-body">
              <h4 class="card-title">
                <%= artists[i].name %>
              </h4>
              <p class="card-text">
                <%= artists[i].dates %>
              </p>
              <a href="<%= artists[i].url  %>" class="btn btn-primary">Visit Gallery</a>
            </div>
          </div>
        </div>
        <% } %>
    </div>
    <%include footer%>
</body>

</html>